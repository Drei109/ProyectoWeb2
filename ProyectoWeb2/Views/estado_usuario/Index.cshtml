@model List<ProyectoWeb2.Models.estado_usuario>
@{
    ViewBag.Title = "Index";
}

<h2 class="page-header"><a href="~/estado_usuario/Agregar" class="btn btn-default pull right">Nueva Estado Usuario</a>Estado Usuario</h2>


@using (Html.BeginForm("Index", "estado_usuario", FormMethod.Post, new { criterio = "frm-buscar" }))

{
    <div class="text-left">
        @Html.Label("Buscar:")
        @Html.TextBox("Criterio")
        <button type="submit" class="btn btn-info" value="Buscar">Buscar</button>
    </div>
}
<table id="ejemplo2" class="table">
    <thead>
        <tr>
            <th style="width:50px">ID</th>
            <th style="width:50px">Nombre</th>
            <th style="width:150px"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th>@(item.estado_usuario_id)</th>
                <th>@(item.nombre)</th>
                <th style="align-content:center;">                    
                    <a href="~/estado_usuario/Agregar/@item.estado_usuario_id" class="btn btn-warning">Modificar</a>
                    <a href="~/estado_usuario/eliminar/@item.estado_usuario_id" class="btn btn-danger" onclick="return confirm('¿Esta seguro de eliminar el registro?');">Eliminar</a>
                </th>
            </tr>
        }
    </tbody>
</table>